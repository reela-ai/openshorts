services:
  backend:
    build: .
    container_name: openshorts-backend
    ports:
      - "13002:8000"
    volumes:
      - .:/app
      - /app/__pycache__
      - /mnt/data/tmp/openshorts-output:/app/output
      - /mnt/data:/mnt/data:ro
    restart: unless-stopped
